# Agile Backlog

Traceability: every item cites its source (Doc = co-founder document table,
WB = workbook sheet, DIR = directive section, AUD = legacy audit finding).

## Definition of Ready / Done — see definition-of-ready-done.md

## Epics

| Epic | Goal | Phase |
|---|---|---|
| E1 Foundation | Repo, CI, framework, schema, API core | 1 |
| E2 Identity & Tenancy | Hardened auth, org context, isolation proof | 1 |
| E3 Candidate Experience | Invitation → disclosure → workspace → submit → rights | 2 |
| E4 Review & Evidence | Queue → scoring → ledger → adjudication → finalisation | 2 |
| E5 Employer Experience | Jobs, candidates, imports, profiles, communications | 2 |
| E6 Platform Administration | Employer CRM, entitlements, support, flags, compliance ops | 3 |
| E7 Learning | Courses, pathways, learner portal, skills | 4 |
| E8 Workforce Intelligence | Privacy-first analytics | 5 |

## Sprint-ready stories (extract; IDs referenced across all docs)

### Done (this cycle)
- **CPF-01** ✅ Source import pipeline (WB) — 10 templates normalised, fidelity-tested.
- **CPF-02** ✅ Scoring engine with governance controls (WB Scoring Model) — 12 tests.
- **CPF-03** ✅ Lifecycle state machines incl. disclosure + report gates (Doc T21) — 14 tests.
- **CPF-04** ✅ Migrations 0001–0004: tenancy FORCE RLS, audit chain, assessment core, identity runtime, app role (Doc T17, AUD L2/L6).
- **CPF-05** ✅ Framework API + stateless evaluation + error contract (DIR §13) — 8 tests.
- **CPF-06** ✅ CI with real-PG migration + integration validation.
- **CPF-40** ✅ Identity module: scrypt, activation, lockout, TOTP MFA, session revocation (Doc T20, AUD L3) — integration-tested; SSO/OIDC remains planned.
- **CPF-42** ✅ Tenant-context middleware + API isolation matrix + RLS backstop test (DIR §15).
- **CPF-20** ✅ Disclosure API with versioned notices — gate enforced end-to-end.
- **CPF-22** ✅ Report issuance behind finalised human review — tested at API + DB layers.
- **CPF-23** ✅ Evidence-profile projection (bands/claims/probes, integrity separated, no index).
- **CPF-24** ✅ Evidence ingestion API: category allow-list, forbidden-event rejection, active-session precondition.
- **CPF-25** ✅ Data-rights workflows incl. candidate-raised erasure + legal holds (portal + org side).
- **CPF-27** ✅ Org read-model endpoints for the web UI: sessions pipeline, member directory, review detail (org/views.ts) — integration-tested incl. tenant isolation.
- **CPF-36** ✅ Web application: routing/auth/shell/session-restore + all 13 pages implemented against the real API — sign-in, platform employer directory, candidate CRM, template library, job profiles, sessions pipeline, team directory, data rights + legal holds, review queue, reviewer workspace (rubric scoring/preview/finalise), evidence profile (bands/probes, no verdict), candidate entry, and the full public candidate portal (disclosure gate, evidence workspace, real integrity signals, DSR self-service). 42 component/unit tests incl. an 11-test vitest-axe accessibility smoke suite; typechecked/built/browser-smoke-verified against the real API. No stub pages remain.
- **CPF-34** ✅ Employer responsible-use acknowledgement (Doc T22-P2) — migration 0005 (employer_acknowledgements, RLS+FORCE); GET/POST /v1/orgs/:orgId/acknowledgements/responsible-use; Evidence Profile endpoint returns 428 ACKNOWLEDGEMENT_REQUIRED until the viewer has acked the current document version; version-mismatch re-ack returns 409 STALE_DOCUMENT_VERSION; web interstitial gate before first profile view. Integration-tested (428→ack→200; stale version rejected).
- **CPF-21** ✅ **CPF-31** ✅ Evidence Ledger claims API + Doc T12 band developer-rules validation — GET/POST/PUT/DELETE `/v1/orgs/:orgId/reviews/:reviewId/claims(/:claimId)` on the existing `evidence_ledger_claims` table (migration 0002), reviewer-only + own-review-only, frozen (409) once the review is finalised; evidence references validated against the session's real `evidence_events` in a single `ANY($ids)` query; structural band rules enforced via `@cpf/assessment-framework`'s new `checkBandRule()` (Exceptional ≥2 refs + high confidence, Strong ≥2 refs, Some requires counter-evidence + limitations). Integration-tested (fake reference → 422; under-strength Strong → 422; full CRUD lifecycle; frozen-after-finalisation → 409).
- **CPF-30** ✅ AI Collaboration Profile rendering (7-dimension lens, ADR-0004) — evidence-profile endpoint now returns `collaborationProfile`: 7 dimensions each with the strongest band among the review's ledger claims (or "Not assessed"), and per-claim `{claim, band, limitations, counterEvidence}` — never evidence references or reviewer confidence, keeping employer-facing output free of raw evidence/session internals. Reviewer workspace gained a claims editor panel (create/edit/delete, evidence-reference checklist scoped to the session's real evidence events, band select with Doc T12 rule hints), frozen once the review is finalised. Evidence Profile page renders the collaboration profile section above the scoring dimension bands. Integration-tested (claims text present, event ids/payloads never present) and web-tested (7-dimension section renders, heading precedes "Dimension bands", no `evidenceReferences`/`reviewerConfidence` leakage).
- **CPF-33** ✅ Reviewer calibration records + assignment gating (Doc T22-P1) — migration 0006 (reviewer_calibration_records: org, reviewer, framework_version, status valid/expired/revoked, calibrated_at/expires_at, RLS+FORCE); admin endpoints POST/GET `/v1/orgs/:orgId/reviewer-calibrations` + DELETE (revoke); the existing session-review-assignment endpoint now checks `checkReviewerCalibrated()` and returns 422 `REVIEWER_NOT_CALIBRATED` unless the reviewer holds a valid, non-expired record for the session template's `frameworkVersion`; TeamPage gained a Calibration column (status pill + "Record calibration" action) for reviewer-role members. Integration-tested (uncalibrated assign → 422; calibrated → 201; expired record → 422; revoke → 204). Web-tested (calibration pill + record flow).
- **CPF-26** ✅ Retention sweep job — `apps/api/src/jobs/retention.ts`, runnable as `npm run retention:dry-run`/`retention:execute` in `apps/api` (also importable/testable as `runRetentionSweep()`). For each organisation with a configured `retention_policies` row, deletes `evidence_events` (by category — `workspace_evidence`/`integrity_signal`, each against its own retention-day setting) whose owning session is in a terminal status (`report_issued`/`withdrawn`/`expired`/`invalidated`) and past the window measured from the session's terminal `updated_at`, and anonymises candidates whose sessions are all terminal and past the evidence window — all suppressed entirely for any candidate under an active legal hold. Defaults to a dry run (counts only); `--execute`/`{execute: true}` applies. A per-category, per-org deletion cap (default 5000, configurable) skips an org's execution outright (still reporting counts) if exceeded, guarding against a mass-delete from a misconfigured policy. Writes one `retention.sweep_executed` audit entry per org per run with counts. `audit_log` and ledger/score data are untouched (retained for accountability; only removed via the explicit data-rights erasure workflow). Scheduling (cron/Task Scheduler) documented in the operations runbook. Integration-tested: dry-run reports without deleting, execute deletes only aged+unheld data, legal hold suppresses, cap guard skips execution, audit entry recorded, residual checks confirm zero remaining events + anonymised candidate for the eligible fixture and untouched data for the held fixture.
- **CPF-37** ✅ Notification delivery via mail adapter — migration 0008 (`outbound_messages`: org-scoped, RLS+FORCE, status `queued`/`sent`/`failed`/`dead_letter`, attempts, `next_attempt_at`). `modules/notifications/mail.ts`: `MailPort` interface, `consoleAdapter` (default; logs recipient+subject metadata only, NEVER the body), `smtpAdapter` (nodemailer, used only when `SMTP_HOST` is configured); `templates.ts`: HTML-escaped invitation-issued (courier note to the inviting hiring manager — direct-to-candidate delivery is out of scope for this pass, no consent/enablement setting exists yet), activation-token-issued, DSR received/due-soon templates, never embedding the actual access/activation tokens. `modules/notifications/queue.ts`: `enqueueOutboundMessage` + `processOutboundQueue` (exponential backoff, 60s doubling per attempt, `dead_letter` after 5 attempts, audits every sent/dead-lettered transition). `jobs/notify.ts`: per-org retry driver + CLI (`npm run notify:retry`). Wired into the real invitation-creation and user-invite flows (`hiring.ts`, `platform/routes.ts`) so both now enqueue a notice as part of the same transaction. Scheduling documented in the operations runbook. Unit-tested (template HTML-escaping, no raw tokens in bodies, console adapter never logs the body) and integration-tested (real invitation issuance enqueues a message; synthetic fixture proves fail→backoff→retry→sent and fail×5→dead_letter, with audit entries for both terminal outcomes).
- **CPF-38** ✅ Double-scoring & adjudication workflow completion — migration 0007 (`reviews.second_reviewer_user_id`); new admin endpoint `POST /v1/orgs/:orgId/reviews/:reviewId/second-reviewer` (validates reviewer role, calibration, distinct from reviewer1, review not finalised); PUT `/v1/orgs/:orgId/reviews/:reviewId/scores` rewritten with actor-based field guards — reviewer1 may only write `reviewer1Score`, reviewer2 only `reviewer2Score`, org admin only `adjudicatedScore` (and only once both reviewer scores exist for that criterion) — returning 403 `FORBIDDEN`/422 `ADJUDICATION_SCORE_PREMATURE` otherwise; fixed a latent bug where the prior blanket UPSERT wrote all score columns on every save (a second writer would have nulled out the first reviewer's stored score — caught and fixed before any two-reviewer traffic existed). `@cpf/assessment-framework`'s `resolveScore()` already implemented the variance/adjudication resolution end-to-end and needed no changes. Web: SessionsPage gained an "Assign second reviewer" action + modal with a 20–30% sampling-guidance note; ReviewWorkspacePage is now actor-aware (reviewer1/reviewer2/admin), showing the other party's already-saved score read-only and restricting the writable radio group + save payload to the caller's own field. Integration-tested end-to-end with two real reviewer identities (second-reviewer assignment, same-reviewer rejection, forbidden-field 403s for both reviewers and admin, premature-adjudication 422, variance → blocked finalise → adjudication → finalise).
- **CPF-35** ✅ Candidate CSV import with partial-failure reports + dedupe — `POST /v1/orgs/:orgId/candidates/import` (`text/csv`, ≤1MB route `bodyLimit`, ≤2000 data rows) parses a `name,email` header CSV (RFC4180-style quoting via a small hand-rolled parser in `modules/org/csv.ts`, BOM-tolerant), validates each row (zod e-mail/name checks), de-dupes case-insensitively both within the file and against existing `(organisation_id, email)` candidates, inserts valid rows transactionally, and returns `{created, skippedDuplicates[{line,email}], invalid[{line,reason}]}` — partial success is a 201, never a partial failure. Candidate names starting with a formula-trigger character (`=+-@`/tab) are prefixed with a leading apostrophe before storage (`neutraliseCsvFormula`), pre-empting CSV/formula injection if these values are ever re-exported to a spreadsheet later. Row-count overage and oversized bodies both return 413 (`IMPORT_TOO_LARGE` / `PAYLOAD_TOO_LARGE`, the latter via a new app-wide `FST_ERR_CTP_BODY_TOO_LARGE` → error-contract translation). One `hiring.candidates_imported` audit entry per import with created/duplicate/invalid counts. Web: CandidatesPage gained an "Import CSV" button opening a file-upload modal that reports the partition and offers a downloadable CSV of the rejected rows (duplicates + invalid, with reasons). Integration-tested (mixed valid/in-file-duplicate/existing-duplicate/invalid/formula-injection file → correct partition and neutralisation; >2000 rows → 413; >1MB body → 413; audit entry recorded). Web-tested (renders the partition report; surfaces the server error message on failure).
- **CPF-43** ✅ Rate limiting + Idempotency-Key middleware — migration 0009 (`idempotency_keys`: not RLS-protected, a duplicate-suppression cache scoped explicitly by `(scope, actor_key, idempotency_key)` in application code, the same pattern already used for the platform-level `organisations` table). `modules/rate-limit.ts`: `InMemoryRateLimitStore` (token bucket, single-node — honestly documented, not distributed — `RateLimitStore` is the seam for a future Redis-backed store); wired as an `onRequest` hook in `app.ts`, keyed per bearer session token when authed or per-IP otherwise, with a stricter bucket for `/v1/auth/*` and `/v1/candidate/*` (reachable without prior authentication); exceeding the bucket returns 429 with a `retry-after` header and the standard error contract. Bucket sizing is configurable per `buildApp()` call (generous production defaults; a dedicated small-bucket app instance is used in the rate-limit integration test rather than tuning the shared test app's buckets down, to avoid flaking the rest of the suite) plus a `RATE_LIMIT_TEST_MULTIPLIER` env knob for the real server process. `modules/idempotency.ts`: `runIdempotent()` — stores a request-body hash + response on first execution, replays the identical stored response on a matching retry (including surviving a concurrent-request race via a unique-constraint-violation fallback re-read), and throws `IdempotencyConflictError` (→ 422) when the same key is replayed with a different body. Wired via an optional `Idempotency-Key` header on invitation creation, candidate CSV import, and candidate-raised data-rights requests. Integration-tested (burst against a small dedicated bucket → 429 + `retry-after`; replayed candidate-import request returns the identical stored response with no duplicate candidate row; same key + different body → 422, no mutation).
- **CPF-44** ✅ OpenAPI generation from the live route table — `src/openapi.ts`: `captureRoutes(app)` hooks Fastify's `onRoute` event so every route registered anywhere (including via imported `register*Routes(app)` functions) is captured automatically, self-maintaining by construction rather than hand-transcribed; `buildOpenApiSpec()` builds an OpenAPI 3.0.3 document documenting each route's tag, auth requirement (`bearerAuth` unless an explicitly allow-listed public route: health, framework catalogue, scoring evaluation, login, candidate-portal routes, first-admin bootstrap), and the standard `{error:{code,message,requestId,retryable}}` envelope for its plausible status codes — deliberately does not attempt full per-route zod-schema derivation (out of scope for this pass; the inject-based integration/unit test suite remains the source of truth for exact request/response field shapes, honestly documented in the spec's own description). Served live at `GET /v1/openapi.json`. `apps/api/scripts/generate-openapi.mjs` (new npm script `openapi:generate`) boots the built app and writes the response to the committed `docs/api/openapi.json` artifact; a new CI step regenerates the spec after build and fails the build on `git diff --exit-code` drift. Integration-tested: spec is valid per `@apidevtools/swagger-parser`, and asserts specific real routes (candidate CSV import, login, candidate data-rights) plus an overall route-count floor are present in `spec.paths`.
- **CPF-45** ✅ Supply-chain & static analysis in CI — new `.github/workflows/security.yml`: `secret-scan` job (gitleaks, full git history via `fetch-depth: 0`); `codeql` job (`javascript-typescript`, `security-events: write`); `sbom` job (generates a CycloneDX SBOM via `@cyclonedx/cyclonedx-npm`, uploaded as a 90-day build artifact); `dependency-review` job (PR-only, fails on high-severity advisories). `npm audit` was already present in the main CI workflow. While wiring up the SBOM job locally, discovered and fixed a genuine pre-existing `package-lock.json` integrity defect: `@types/react`'s declared dependency on `csstype@^3.2.2` had no corresponding resolved package entry anywhere in the lockfile, so it was silently never installed (`npm ls --all` — which the SBOM tool depends on — failed with `ELSPROBLEMS: missing: csstype`), even though `npm ci`/typecheck/build/tests all otherwise passed without visibly surfacing it; fixed by reinstalling `@types/react` to force npm to re-resolve the full dependency graph, which correctly added `csstype@3.2.3`. Not run against a live GitHub Actions runner in this environment (no push access) — same disclosed limitation as the rest of the CI workflow additions; the SBOM/lockfile-repair portion was verified locally.
- **CPF-46** ✅ Session & authz hardening — migration 0010 adds `auth_sessions.absolute_expires_at` (hard 24h cap from login, set explicitly in the login INSERT) and `auth_sessions.stepped_up_at` (marks the most recent full re-authentication). `requireAuth()` (`modules/auth/guards.ts`) now performs sliding renewal on every authenticated request — `expires_at = LEAST(now() + 12h, absolute_expires_at)` — so activity keeps a session alive but it can never outlive the 24h absolute cap regardless of how active the caller is. New `DELETE /v1/orgs/:orgId/users/:userId/roles/:role` admin endpoint (`platform/routes.ts`) removes an `org_memberships` row and, per the simplest-safe mitigation for mid-session capability changes, revokes ALL of the affected user's active sessions (rather than attempting to surgically downgrade a live session) — audited as `org.role_removed` with the revoked-session count, and refuses (409 `LAST_ORG_ADMIN`) to remove an organisation's last `org_admin` so an org can never lock itself out. New step-up authentication mechanism: `POST /v1/auth/step-up` (`auth/routes.ts`) re-verifies the caller's password (plus TOTP if MFA-enrolled) and marks the *current* session as freshly authenticated (`stepped_up_at = now()`) without issuing a new bearer token; a new `requireFreshAuth` guard composes after `requireOrgRole` in a route's `preHandler` array and returns 401 `STEP_UP_REQUIRED` if the session's `stepped_up_at` is older than `STEP_UP_FRESHNESS_MINUTES` (5). Applied to a new org data-export endpoint, `GET /v1/orgs/:orgId/export` (`org/views.ts`) — an admin-only, audited (`org.data_exported`, counts only) bulk JSON export of the org's candidates/sessions/reviews, the first such export capability in the codebase. Web-side re-prompt UI for `STEP_UP_REQUIRED` was **not** built this step (no export/report UI surface currently calls this endpoint) — noted honestly as scoped-out; the API contract is stable and ready for a future UI to consume. Integration-tested: fresh login exports immediately; an artificially-aged `stepped_up_at` triggers 401 `STEP_UP_REQUIRED`; a failed step-up attempt (wrong password) returns 401 `STEP_UP_FAILED`; a successful step-up re-verification restores export access; role removal immediately invalidates the affected user's session (`/v1/auth/me` → 401) and is idempotent-safe (repeat removal → 404); removing an organisation's sole remaining `org_admin` is rejected with 409; sliding renewal is verified to clamp `expires_at` to an artificially-close `absolute_expires_at` rather than extending a fresh 12h window.
- **CPF-47** ✅ Authorization matrix automation (Delivery Plan Step 28) — new `apps/api/test/authz-matrix.test.ts`: a table-driven test asserting deny-by-default across every one of the 37 org-scoped route+method combinations (all 30 distinct `/v1/orgs/:orgId/...` path templates) against all 7 caller types (`org_admin`, `hiring_manager`, `reviewer`, `learning_admin`, `support_agent`, no token, and a cross-org `org_admin`). The route→role table is hand-derived from every `requireOrgRole(...)`/`requireFreshAuth` `preHandler` registration in the source, then cross-checked at test time against the live `GET /v1/openapi.json` spec so any newly-added or removed org-scoped route breaks the test immediately (fails loudly listing the exact missing/stale route) rather than silently going unverified. To keep the matrix fast and avoid needing full business-logic fixtures for all 30 route shapes (sessions, reviews, claims, legal holds, calibrations, etc.), each cell sends a minimal dummy body/path-param and asserts only the authorization boundary: an authorized caller's response must never be 401/403 (whatever business-logic status follows is out of scope here — already covered by `integration.test.ts`), while an unauthorized caller must get exactly 401 (no token) or 403 (wrong role / cross-org). One special case: `POST /v1/orgs/:orgId/candidates/import` requires a `text/csv` body instead of JSON. Confirmed `learning_admin` and `support_agent` are correctly denied by every single route (deny-by-default for roles not yet wired to any endpoint). 39 tests, runs in ~4.7s (well under the plan's 60s target), stable across repeated runs; full API suite now 67 passed/1 skipped (up from 28/1), root suite 197 passed/1 skipped. No CI wiring needed beyond what already exists — the file is picked up automatically by the existing `vitest run` test script that both local `npm test` and CI's `verify`/`integration` jobs already invoke.
- **CPF-48** ✅ Threat-model refresh + security review checkpoint (Delivery Plan Step 29, MILESTONE) — `docs/security/security-architecture-and-threat-model.md` rewritten with a per-threat `Status` column reflecting implemented reality (T1–T4) and a corrected "not yet implemented" gap list (rate limiting/MFA/sessions moved to implemented; CSRF explicitly noted as not applicable since the web client is bearer-token authenticated via `Authorization` header + `sessionStorage`, never cookies, so there is no ambient credential for a cross-site request to ride). New `apps/api/test/security-hardening.test.ts`: (1) a redaction test — both a direct unit test of the exact `LOG_REDACT_PATHS` config (now a shared constant in `modules/constants.ts`, used by both the real server logger and the test) against a raw pino instance, and an end-to-end test that builds a real app with a captured log stream and confirms a genuine bearer token is never emitted for an authenticated request; (2) an ingestion fuzz suite against the candidate evidence-events endpoint covering malformed JSON, an oversized body at both the transport (`bodyLimit`, 413) and application (`MAX_EVENT_PAYLOAD_BYTES`, 413) layers, unicode (emoji/CJK/RTL, round-tripped through Postgres and confirmed byte-faithful), and prototype-pollution-shaped keys (`__proto__`, `constructor.prototype`) — confirmed the payload is always stored as inert jsonb data via a parameterized insert (never merged/evaluated) and that `Object.prototype` of the running process is never actually mutated. The fuzzing exercise found and fixed a real bug: the global Fastify error handler fell through to a blanket `500 INTERNAL_ERROR` for any error it didn't explicitly special-case, including Fastify's own correctly-classified `400` for malformed JSON bodies — now any Fastify-assigned 4xx `statusCode` not already special-cased is honestly forwarded instead of masked as a server fault. `docs/status/completion-report.md` updated (fifth build cycle) per the milestone requirement. Full verification green: api typecheck, api suite x2 (74 passed/1 skipped, up from 67/1), root typecheck, root test (204 passed/1 skipped), root build, web vite build, npm audit (0 vulns). New explicit devDependency: `pino` (already a transitive dependency via fastify; declared directly since the test imports it to build a raw logger instance for the redaction unit test).

- **CPF-49** ✅ GitHub push + CI first run (Delivery Plan Step 30) — added `origin` = `https://github.com/ANSHU-Ireland/CPF-2.0.git` (user-supplied remote) and pushed `main` for the first time (650 objects). Both workflows fired on the push and went green on the first real run: `ci` → `verify` (46s) + `migrations` (55s) both passed against real PG16 in the CI container (no PG16-vs-PG17 drift surfaced); `security` → `codeql` (1m24s), `sbom` (21s), `secret-scan`/gitleaks (8s) all passed, with `dependency-review` correctly skipped (it's PR-only; this was a push event) — verified via `gh run list`/`gh run view`. Branch protection (PR review + required `ci` check per CONTRIBUTING.md) was deliberately deferred: enabling it now would force every remaining delivery-plan step through a PR instead of a direct commit, so the user was asked and chose to skip it for now, revisiting before any real collaborator or pilot. Attempted to enable GitHub's native repo-level security toggles (Dependabot vulnerability alerts, secret-scanning + push-protection) via the API, but the authenticated `gh` CLI identity only has `pull` access on this repo (not `admin`/`push`) — a different, lower-privileged account than whatever credential performed the actual `git push` — so those toggles require someone with admin access on github.com directly; disclosed honestly rather than worked around.

- **CPF-54** ✅ Subscriptions & entitlements model (Delivery Plan Step 35) — migration 0012 (deliberately numbered to match the plan's own scheme and avoid colliding with migration 0011, which is reserved by the not-yet-merged Step 32 branch): platform-owned `plans` (code, name, `module_entitlements` jsonb, `limits` jsonb) and `org_subscriptions` (organisation_id unique fk, plan_id fk, status active/suspended/cancelled, period) tables — no RLS, same treatment as `organisations` itself, since only the platform_admin API ever writes them. Seeded an `internal-pilot` plan (assessments-only entitlement; generous limits) idempotently in the migration itself. New `apps/api/src/modules/platform/subscriptions.ts`: `GET`/`POST /v1/platform/plans` (list/create), `POST /v1/platform/orgs/:orgId/subscription` (assign/change plan, upserts the subscription row), `POST /v1/platform/orgs/:orgId/suspend` and `.../resume` (flip `organisations.status` — the pre-existing `organisation_status` enum already had a `suspended` value from migration 0001 that nothing enforced until now — and mirror the status onto `org_subscriptions` if a row exists), all platform_admin-only, all audited. Enforcement lives in the single existing `requireOrgRole` chokepoint every org-scoped route already runs through (guards.ts): after the role/membership check, one additional indexed primary-key lookup on `organisations.status` returns 403 `ORG_SUSPENDED` for every org-scoped route if the organisation is suspended — a single query added once per request, not a repeated check, addressing the plan's own stated performance risk. Candidate-portal routes are structurally unaffected (they authenticate via a per-candidate token, never org membership, so they never pass through this guard). New `apps/api/test/subscriptions.test.ts` (9 tests): plan listing/creation (and platform_admin-only enforcement), plan assignment (including 404 on an unknown plan code), suspend → every org-scoped route now 403 `ORG_SUSPENDED` (checked on two distinct routes) while a separate, active organisation is completely unaffected, resume → routes work again, and 404s for suspend/resume against a non-existent organisation. `docs/api/openapi.json` regenerated (new routes auto-captured, no manual spec edits needed, per Step 24's self-maintaining design). Root typecheck clean, root test 204 passed/1 skipped → 213 passed/1 skipped with the new file (x2 consecutive runs against local PG17 — 65 framework + 17 identity + 83+1skip API + 48 web), root build clean, web vite build clean, `npm audit` 0 vulnerabilities.

- **CPF-55** ✅ Entitlement enforcement (Delivery Plan Step 36) — new shared `apps/api/src/modules/platform/entitlements.ts` (`getOrgPlan`, `isModuleEntitled`, `DEFAULT_MODULE_ENTITLEMENTS`): resolves an org's active `org_subscriptions`→`plans` row, defaulting an unsubscribed org to the pre-Step-35 baseline (`assessments: true`, everything else `false`) so existing/legacy organisations are never silently locked out by the new commercial model. New `requireModuleEntitlement(moduleKey)` guard (`auth/guards.ts`) composes after `requireOrgRole` in a route's `preHandler` array and returns 403 `MODULE_NOT_ENTITLED` if the org's plan doesn't grant that module — applied across the whole assessments-module route surface (`hiring.ts`, `reviews.ts`, `calibration.ts`, `claims.ts`, `acknowledgements.ts`, and the assessment-related routes in `views.ts`: sessions pipeline, review detail, org export), deliberately left off the org member directory (`/users`) and `data-rights.ts` (legal DSR rights, not a commercial module). Plan-limit enforcement: invitation creation (`hiring.ts`) now checks the org's `plans.limits.maxActiveAssessments` (if the plan specifies one) against the count of non-terminal invitations before creating a new one, returning 422 `PLAN_LIMIT_REACHED` with upgrade guidance once reached — orgs with no subscription, or a plan that doesn't specify the limit, are unrestricted. New `GET /v1/orgs/:orgId/usage` (org_admin, `org/usage.ts`): active-assessment and org-user counts vs. plan limits, deliberately NOT module-gated so an org can always see why it's blocked / what to upgrade. New placeholder `GET /v1/orgs/:orgId/learning/status` (`org/learning.ts`) for the not-yet-built Learning module — exists solely to prove the entitlement gate works end-to-end for a module with zero real feature routes, the same guard will front the real Learning feature set once built. Web: added a "Plan & usage" card to the existing Team page (new `OrgUsage` type in `api.ts`) showing plan code and used/limit for active assessments and team members; limit errors on invitation creation were already surfaced for free via the existing generic error-message rendering in `CandidatesPage.tsx`'s invite form. New `apps/api/test/entitlements.test.ts` (5 tests): default-baseline org can create assessments/invitations without any subscription; Learning route 403 `MODULE_NOT_ENTITLED` by default, then 200 once a plan granting `learning: true` is assigned; `maxActiveAssessments: 1` plan blocks a second invitation with 422 `PLAN_LIMIT_REACHED`; usage endpoint reports counts vs. the assigned limit. `authz-matrix.test.ts` extended with the two new routes (39 → 41 route cases) and its fixture org given a full-access test plan so the matrix continues to test purely the role boundary (plan boundary is entitlements.test.ts's job). `docs/api/openapi.json` regenerated. Root typecheck clean, root test 213 passed/1 skipped → 220 passed/1 skipped (x2 consecutive runs against local PG17 — 65 framework + 17 identity + 90+1skip API + 48 web), root build clean, web vite build clean, `npm audit` 0 vulnerabilities.

- **CPF-56** ✅ Support console / JIT access (Delivery Plan Step 37) — migration 0013: `support_access_grants` (organisation_id, platform_user_id, `scope` enum `read_metadata`/`read_evidence`, reason, `status` pending/approved/denied/revoked, `break_glass` flag, `approved_by_org_admin` nullable, `requested_at`/`expires_at` bounded to ≤4h via a CHECK constraint, `revoked_at`) — platform-owned, no RLS, same treatment as `plans`/`org_subscriptions`. New `apps/api/src/modules/platform/support-access.ts`: `POST /v1/support/orgs/:orgId/access-grants` (platform_admin only — requests a time-boxed grant; an optional `breakGlass: true` immediately self-approves for genuine emergencies, distinctly audited), `POST /v1/support/orgs/:orgId/access-grants/:grantId/approve` (org_admin of that org only, requires the grant still be `pending`), and `GET /v1/support/orgs/:orgId/summary` — a metadata-only read (invitation count, org-user count, session counts by status; **never** evidence content, regardless of the grant's `scope`, since no endpoint exposing evidence content exists yet) gated by a new `requireSupportAccess` guard (`auth/guards.ts`) that checks for an active (approved, unexpired, unrevoked) grant rather than org membership — deliberately a different authorization shape from `requireOrgRole`, since a platform support agent is never an org member. Every request, approval, break-glass grant, and summary access writes **two** audit entries — one scoped to the organisation (`organisationId: orgId`), one scoped platform-wide (`organisationId: null`) — so either audience can see the complete picture from their own vantage point. These routes live under `/v1/support/orgs/:orgId/...` (not `/v1/orgs/:orgId/...`), so they fall outside `authz-matrix.test.ts`'s automated cross-check by design (that matrix is purpose-built for the standard `requireOrgRole(...roles)` membership model with a fixed caller set; support access is grant-based and actor-is-not-a-member, which doesn't fit its fixture shape) — authorization coverage instead lives in a dedicated new `apps/api/test/support-access.test.ts` (7 tests: no grant → 403; pending-only grant → still 403; approved → 200 + both audit scopes populated for every step; force-expired grant → 403 again; break-glass self-approval works and is dual-logged with distinct action names; a non-platform-admin can't request; a non-owning-org-admin can't approve). Root test suite 220 passed/1 skipped → 227 passed/1 skipped (x2 consecutive stable runs), root typecheck/build clean, `docs/api/openapi.json` regenerated.

- **CPF-57** ✅ Compliance operations console (Delivery Plan Step 38) — new `apps/api/src/modules/org/compliance.ts`, all org_admin-only: `GET /v1/orgs/:orgId/audit/search` (paginated/filtered by `entityType`/`action`/`from`/`to`, always scoped by `organisation_id` since `audit_log` is deliberately NOT RLS-protected); `GET /v1/orgs/:orgId/audit/export` (additionally gated by `requireFreshAuth`, per the plan's own risk note that an export is a personal-data-egress event) streams a CSV of the filtered slice (capped at 5,000 rows), reusing the existing `neutraliseCsvFormula` CSV-injection defence plus standard quote/comma escaping, and audits the export itself (`compliance.audit_exported`, row count + filters); `GET`/`PUT /v1/orgs/:orgId/retention-policy` read/write the pre-existing (but previously endpoint-less) `retention_policies` table, with the write validated (positive integers ≤3650 days, `deletionMode` enum) and audited (`compliance.retention_policy_updated`); the GET also reports the most recent `retention.sweep_executed` audit entry as `lastRun`, with an honest disclaimer that there's no scheduler-tracking table so no guaranteed next-run time is claimed. `authz-matrix.test.ts`'s ROUTE_TABLE was extended with all four new `/v1/orgs/{orgId}/...` routes (they fall inside its automated cross-check, unlike Step 37's `/v1/support/...` routes). New `apps/api/test/compliance.test.ts` (8 tests, stable across repeated runs): search pagination/filtering by action and entity type; export denied (401 `STEP_UP_REQUIRED`) on a session whose `stepped_up_at` has aged past freshness, then allowed (200, correct CSV headers/content) once stepped up, with the export itself verifiably audited; retention policy GET/PUT round-trip, PUT validation-bounds rejection, and its audit trail. Test fixtures seed audit rows through the real `appendAudit()` helper (not raw SQL with a made-up hash) so the global hash chain stays valid for `integration.test.ts`'s separate chain-verification test, and use a random per-run suffix in seeded action/entity names since `audit_log` is append-only and accumulates across repeated local test runs. Web: single `CompliancePage.tsx` (one route, three sections, a deliberate simplification of the plan's three-page framing since the functional intent is fully met by one page) — "Audit explorer" (filter form, paginated results table, a "Verify chain integrity" button calling the existing `/audit/verify-chain` endpoint, and a CSV export button using a new `api.getBlob()` client helper that surfaces `STEP_UP_REQUIRED` as an actionable message, since no step-up re-prompt UI exists anywhere in the web app yet — a longstanding gap, not addressed here); "Retention dashboard" (last-sweep display + a policy editor with client-side bounds validation, submitted via `noValidate` so the custom JS validation — not native HTML5 constraint validation — is what actually runs and is what's tested); "Data rights SLA" (aging buckets — Overdue/Due within 3 days/Due within 7 days/On track/Resolved — computed client-side from the existing `GET /v1/orgs/:orgId/data-rights` endpoint's `due_at`/`resolved_at`/`overdue` fields, no new endpoint needed per the plan). New nav link for org_admin in `Shell.tsx`, new `orgCompliance` route builder in `routes.ts`. New `apps/web/test/CompliancePage.test.tsx` (5 tests) covering the plan's explicit ask — policy-editor bounds validation (rejects 0 and values above 3650 client-side with no PUT sent, accepts and submits a valid value) — plus section rendering and pre-fill. Full verification: root typecheck clean, root test suite green twice consecutively (assessment-framework 65, identity 17, api 109/1 skipped, web 53), root build clean, `apps/web` `vite build` clean, `npm audit` 0 vulnerabilities, `docs/api/openapi.json` regenerated from a fresh `apps/api` build.

- **CPF-58** ✅ Platform analytics + reviewer-minutes telemetry (Delivery Plan Step 39, MILESTONE) — migration 0014 adds `reviews.started_at`, set the moment a reviewer saves their first score (alongside the pre-existing `finalised_at`), so `finalised_at − started_at` is a real, measurable "reviewer minutes" figure rather than an estimate. New `apps/api/src/modules/org/analytics.ts`: `GET /v1/orgs/:orgId/analytics` (org_admin-only, own-org data only) returns assessments-by-status, per-template `{sessionCount, medianReviewerMinutes}` (median via `percentile_cont`), a completion rate (`report_issued` / sessions with a `started_at`), and a candidate-level "challenge rate" (distinct candidates with a `data_rights_requests` `challenge`-type request ÷ distinct candidates with at least one `report_issued` session) — every "no data yet" case returns an explicit `null`, never a fabricated `0`/`0%`. New `apps/api/src/modules/platform/analytics.ts`: `GET /v1/platform/analytics` (platform_admin-only) aggregates across every organisation; per-template cells are suppressed (`sessionCount`/`medianReviewerMinutes` forced to `null`, `suppressed: true`) unless at least 5 distinct organisations have used that template, preventing a low-cardinality cross-tenant cell from being read back to a single org's private data. This suppression needed a genuinely new capability: `assessment_sessions`/`reviews` FORCE row-level security scoped to `current_org_id()` (migration 0003), so a cross-org platform query legitimately saw zero rows without it — migration 0015 adds a `platform_read_all()` session-local flag added to each table's RLS `USING` clause only (never `WITH CHECK`, so it can never be used to write, move, or delete data across a tenant boundary), set exclusively by this one platform-admin-gated route. New `apps/api/test/analytics.test.ts` (5 tests): org-level figures with real reviewer-minutes and a seeded challenge request; a non-admin org member denied (403); tenant isolation (a second org's analytics are empty, not another org's data); platform-level k-anonymity (5 distinct orgs on one template → not suppressed with correct counts; 2 distinct orgs on another → suppressed with `null` cells); non-platform-admin denied (403). Caught and fixed two real bugs during test authoring: (1) the candidate-portal landing `GET` (which transitions the invitation "sent"→"opened") is a required step before `/accept` — omitting it from the test fixture surfaced a real `409 STATE_CONFLICT`, not a test bug; (2) the platform-suppression test's own fixture data isn't covered by any other suite's `TRUNCATE` cleanup, so repeated local runs silently pushed a template's distinct-org count past the suppression threshold — fixed by having this file clean its own fixture tables in its own `beforeAll` (it runs first alphabetically, so this is always safe). Web: new `AnalyticsPage.tsx` (org dashboard, admin nav) and `PlatformAnalyticsPage.tsx` (platform-admin nav), both rendering every figure's definition inline and a suppressed cell as `"< 5 organisations"` rather than a blank or a zero; 2 new web test files (4 tests) covering real data rendering, empty states, and that a suppressed cell never leaks its real count. `docs/api/openapi.json` regenerated; `docs/status/completion-report.md` updated (sixth build cycle, MILESTONE requirement).

- **CPF-59** ✅ Learning data model (Delivery Plan Step 40) — migration 0016 (numbered past 0014/0015, both already claimed by the still-open Step 39 branch): `courses`/`course_modules`/`lessons` (ordered authoring, draft/published/archived), `pathways`/`pathway_courses` (ordered course lists), `learning_enrollments` (reuses `users` directly, targets exactly one of a course or a pathway, per-enrollment consent timestamp), `lesson_progress` — every table RLS-isolated identically to every other tenant table. Hard rule enforced structurally: no foreign key in this migration references `candidates`/`invitations`/`assessment_sessions`/`reviews`/`criterion_scores` — learning stays fully separate from hiring evidence. New `learningEnrollmentMachine` (`enrolled → in_progress → completed`/`withdrawn`) in `@cpf/assessment-framework`, 3 new unit tests. Data-model only — no new API routes yet (Step 41 adds the CRUD/enrol/progress endpoints on top of this schema).

- **CPF-60** ✅ Learning APIs (Delivery Plan Step 41) — migration 0017: `learning_assessment_attempts` (organisation-scoped, RLS+FORCE, FKs only to `learning_enrollments`/`lessons`/`users`/`organisations` — never to `candidates`/`invitations`/`assessment_sessions`/`reviews`/`criterion_scores`), storing a learner's own practice-mode input + resulting evidence-profile snapshot. `apps/api/src/modules/org/learning.ts` extended from Step 36's placeholder with the full v1 surface: `org_admin`/`learning_admin`-only course CRUD (create/list/detail with nested modules/lessons) + module/lesson authoring (ordered, `position`-unique) + publish (freezes a sha256 checksum of the module/lesson content into `courses.published_checksum`, after which the structure is locked — 409 `COURSE_NOT_DRAFT` on any further add); pathway CRUD + ordered course-linking; a bulk-enrol endpoint (`courseId` XOR `pathwayId`, skips non-members); learner-facing endpoints open to every org role (`my-enrollments`, lesson-progress upsert — auto-transitions `enrolled → in_progress` via `learningEnrollmentMachine` — and an explicit `complete`, both strictly self-scoped, 403 on another learner's enrolment, 409 on a terminal-state re-completion); and a practice-assessment-attempt endpoint that reuses the real `evaluate()` scoring engine (the same one behind `/v1/scoring/evaluate`) against a lesson's linked template code, storing the result in `learning_assessment_attempts` only — v1 scope is explicitly course-enrolment only (a pathway-based enrolment gets a clear 422, honestly disclosed rather than silently unsupported). New `apps/api/test/learning.test.ts` (4 tests): the full author→publish→enrol→progress→complete journey including the locked-after-publish and terminal-state-conflict paths, a pathway authoring+enrol path, module-entitlement enforcement (403 `MODULE_NOT_ENTITLED` for an unsubscribed org), and — the plan's own explicit risk check — a before/after row-count assertion on `reviews`/`criterion_scores` proving a practice attempt never touches either hiring table. `authz-matrix.test.ts`'s ROUTE_TABLE extended with all 15 new `/v1/orgs/{orgId}/learning/...` routes (author-only vs. any-org-role split), keeping the automated route-coverage cross-check exhaustive.

### Next (Phase 1/2 remainder)
None — Phase E (Platform Admin & Commercial, Steps 35-39) is complete and Phase F (Learning) is underway (Steps 40-41 done). Remaining work continues per the delivery plan (Step 42+: Learning UI). Steps 42/44/46/50 are milestone-report checkpoints per the standing plan.





### Phase 2 core remaining (each has full AC in PRD FRs)
- **CPF-32** Candidate workspace UI with approved-AI panel + auto-save/recovery (WB).

### Deferred / gated
- **CPF-50** Desktop integrity agent (Doc T22-P2) — **gated on legal review LR-04+**; session-scoped signals only.
- **CPF-41** SOC 2 evidence collection automation (Doc T19) — Phase 3.

## Sprint 1 proposal (next working cycle)
Goal: "A real org user can sign in securely and the platform proves tenant
isolation end-to-end." → CPF-40, CPF-42, CPF-43 + threat-model update + DPIA
scoping session. Demo: authz matrix test run + audit-chain walkthrough.
