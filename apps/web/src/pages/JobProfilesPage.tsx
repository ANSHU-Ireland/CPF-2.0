import type { ReactNode } from "react";
import { EmptyState } from "../ui.js";

/** Job profiles table + create modal. Built in Phase B (CPF-36). */
export function JobProfilesPage(): ReactNode {
  return (
    <EmptyState
      title="Under construction — CPF-36"
      hint="Job profile management will appear here."
    />
  );
}
