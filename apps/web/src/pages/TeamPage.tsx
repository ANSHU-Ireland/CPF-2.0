import type { ReactNode } from "react";
import { EmptyState } from "../ui.js";

/** Org member directory + invitation. Built in Phase B (CPF-36). */
export function TeamPage(): ReactNode {
  return (
    <EmptyState
      title="Under construction — CPF-36"
      hint="Your organisation's member directory and invitations will appear here."
    />
  );
}
